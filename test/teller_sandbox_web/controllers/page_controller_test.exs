defmodule TellerSandboxWeb.PageControllerTest do
  use TellerSandboxWeb.ConnCase

  #test "GET /", %{conn: conn} do
  #  conn = get(conn, "/")
  #  assert 1 =~ 1
    #assert send_resp(conn, 200) =~ "Hello."
  #end
end
